import * as FA from "react-icons/fa";
import * as BI from "react-icons/bi";

type ExportIcon = {
    FA: typeof FA
    BI: typeof BI
}

const Icon: ExportIcon = {
    FA,
    BI
}

export default Icon;